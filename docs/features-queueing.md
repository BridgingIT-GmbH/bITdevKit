# Queueing Feature Documentation

> Allow applications to handle tasks asynchronously, improving performance and scalability. By decoupling producers and consumers, queueing enables systems to process tasks in the background without blocking the main application flow.