@page "/todos"
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Components.Forms
@using Microsoft.JSInterop
@using Microsoft.Kiota.Abstractions
@using Microsoft.Kiota.Abstractions.Serialization
@using System.Net.Http.Json
@using System.Net.Http.Headers
@using BridgingIT.DevKit.Examples.DoFiesta.Presentation.Gen.Models
@inject BridgingIT.DevKit.Examples.DoFiesta.Presentation.Gen.BackendApiClient Api
@* @inject IStringLocalizer<Global> localizer *@
@inject ISnackbar Snackbar
@inject IJSRuntime JS
@inject HttpClient Http
@attribute [RequireAuthentication]

<PageTitle>DoFiesta - Todos</PageTitle>

<MudContainer MaxWidth="MaxWidth.Large" Style="margin-top: 20px;">
    <MudGrid>
        <MudItem xs="12" Class="d-flex align-center">
            @* <MudSelect T="int" @bind-Value="newTodoPriority">
                <MudSelectItem Value="1">-</MudSelectItem>
                @foreach (var priority in enums.TodoPriorities.Where(e => e.Enabled))
                {
                    <MudSelectItem Value="@priority.Id">@priority.Value</MudSelectItem>
                }
            </MudSelect> *@
            <MudTextField @bind-Value="newTodoTitle"
                          Immediate="false"
                          Label="New Todo"
                          Variant="Variant.Text"
                          Margin="Margin.Dense" />
            <MudTooltip Text="Add Todo">
                <MudIconButton Icon="@Icons.Material.Filled.Add"
                               Variant="Variant.Filled"
                               Color="Color.Secondary"
                               Size="Size.Large"
                               OnClick="@(() => AddNewTodo())" />
            </MudTooltip>
            <MudTooltip Text="Create detailed task">
                <MudIconButton Icon="@Icons.Material.Filled.NoteAdd"
                               Variant="Variant.Outlined"
                               Color="Color.Primary"
                               Size="Size.Large"
                               OnClick="OpenCreateTodoDialog" />
            </MudTooltip>
            <MudSpacer />
            <MudTooltip Text="Refresh">
                <MudIconButton Icon="@Icons.Material.Filled.Refresh"
                               Color="Color.Secondary"
                               Size="Size.Large"
                               OnClick="@(() => LoadItems())" />
            </MudTooltip>

            <MudTooltip Text="Complete all items which are InProgress">
                <MudIconButton Icon="@Icons.Material.Filled.Checklist"
                               Color="Color.Success"
                               Size="Size.Large"
                               OnClick="@(() => CompleteAll())" />
            </MudTooltip>

            <MudTooltip Text="Export TodoItems">
                <MudIconButton Icon="@Icons.Material.Filled.FileUpload"
                               Color="Color.Info"
                               Size="Size.Large"
                               OnClick="@(() => showExportDialog = true)" />
            </MudTooltip>

            <MudTooltip Text="Import TodoItems">
                <MudIconButton Icon="@Icons.Material.Filled.FileDownload"
                               Color="Color.Warning"
                               Size="Size.Large"
                               OnClick="@(() => showImportDialog = true)" />
            </MudTooltip>
        </MudItem>

        <MudItem xs="12">
            @if (enums == null || items == null)
            {
                <MudSkeleton Animation="Animation.Pulse" Width="80%" />
                <MudSkeleton Animation="Animation.Pulse" Width="80%" />
                <MudSkeleton Animation="Animation.Pulse" Width="80%" />
            }
            else
            {
                <MudTable Items="@items" AllowUnsorted="false" Hover="true" Dense="true"
                    Striped="false" Bordered="false" Loading="@loading" LoadingProgressColor="Color.Info">
                    <ToolBarContent>
                        <MudHidden Breakpoint="Breakpoint.Sm" Invert="true">
                            <MudChipSet T="int" SelectionMode="SelectionMode.ToggleSelection"
                                        Color="Color.Primary" CheckMark SelectedValueChanged="OnStatusChipChanged">
                                @foreach (var e in enums.TodoStatuses.Where(e => e.Enabled == true))
                                {
                                    <MudTooltip Text="@e.Description">
                                        <MudChip T="int" Value="@(e.Id ?? 0)">@e.Value</MudChip>
                                    </MudTooltip>
                                }
                            </MudChipSet>
                            <MudDivider Vertical="true" FlexItem="true" />
                            <MudChipSet T="int" SelectionMode="SelectionMode.ToggleSelection"
                                        Color="Color.Secondary" CheckMark SelectedValueChanged="OnPriorityChipChanged">
                                @foreach (var e in enums.TodoPriorities.Where(e => e.Enabled == true))
                                {
                                    <MudTooltip Text="@e.Description">
                                        <MudChip T="int" Value="@(e.Id ?? 0)">>@e.Value</MudChip>
                                    </MudTooltip>
                                }
                            </MudChipSet>
                            <MudSpacer />
                            <MudTextField @bind-Value="filterTitle" Immediate="false" Label="Search"
                                          Variant="Variant.Text" Margin="Margin.Dense" Clearable="true"
                                          Adornment="Adornment.End" AdornmentIcon="@Icons.Material.Filled.Search" OnAdornmentClick="LoadItems" />
                        </MudHidden>
                    </ToolBarContent>
                    <HeaderContent>
                        <MudTh></MudTh>
                        <MudTh Style="width: 40%">Title</MudTh>
                        <MudTh>Status</MudTh>
                        <MudTh>Priority</MudTh>
                        <MudTh Style="width: 25%">
                            <MudTableSortLabel T="string" @onclick="@OnDueDateSortClick"
                                               SortDirection="@currentSortDirection" InitialDirection="SortDirection.Ascending">
                                Due
                            </MudTableSortLabel>
                        </MudTh>
                        <MudTh></MudTh>
                    </HeaderContent>
                    <RowTemplate Context="todoItem">
                        <MudTd>
                            <MudHidden Breakpoint="Breakpoint.Sm" Invert="true">
                                @{
                                    var (icon, color) = GetStatusIcon(todoItem.Status ?? 0);
                                    <MudIcon Icon="@icon" Color="@color" />
                                }
                            </MudHidden>
                        </MudTd>
                        <MudTd DataLabel="Title" Style="width: 100%">
                            <MudTooltip Text="@todoItem.Description">
                                <MudTextField @bind-Value="todoItem.Title" Variant="Variant.Text" Margin="Margin.Dense"
                                              Immediate="true" Class="flex-grow-1" Style="width: 100%; min-width: 350px;"
                                              OnBlur="@(() => UpdateTodo(todoItem))" />
                            </MudTooltip>
                        </MudTd>
                        <MudTd DataLabel="Status">
                            <MudSelect Value="todoItem.Status" T="int?"
                                       ValueChanged="async value => { todoItem.Status = value; await UpdateTodo(todoItem); }">
                                @foreach (var status in enums.TodoStatuses.Where(e => e.Enabled == true))
                                {
                                    <MudSelectItem T="int?" Value="@status.Id">@status.Value</MudSelectItem>
                                }
                            </MudSelect>
                        </MudTd>
                        <MudTd DataLabel="Priority">
                            <MudSelect Value="todoItem.Priority" T="int?" ValueChanged="async value => { todoItem.Priority = value; await UpdateTodo(todoItem); }">
                                @foreach (var priority in enums.TodoPriorities.Where(e => e.Enabled == true))
                                {
                                    <MudSelectItem T="int?" Value="@priority.Id">@priority.Value</MudSelectItem>
                                }
                            </MudSelect>
                        </MudTd>
                        <MudTd DataLabel="Due Date" HideSmall="true">
                            <MudStack Spacing="1">
                                <MudDatePicker Date="ToDateTime(todoItem.DueDate)"
                                               DateChanged="async date => await OnTodoDueDateChangedAsync(todoItem, date)"
                                               DateFormat="yyyy-MM-dd"
                                               Style="min-width: 150px;"
                                               Editable="true"
                                               Placeholder="Select Date" />
                                <MudTextField T="string"
                                              Value="@FormatDueTime(todoItem.DueDate)"
                                              ValueChanged="async value => await OnTodoDueTimeChangedAsync(todoItem, value)"
                                              Label="Due time"
                                              Variant="Variant.Text"
                                              Margin="Margin.Dense"
                                              InputType="InputType.Time"
                                              HelperText="Local time" />
                            </MudStack>
                        </MudTd>
                        <MudTd HideSmall="true">
                            <MudTooltip Text="Attachments">
                                <MudIconButton Icon="@Icons.Material.Filled.AttachFile"
                                               Color="Color.Info"
                                               Size="Size.Small"
                                               OnClick="@(() => OpenAttachmentsDialogAsync(todoItem))" />
                            </MudTooltip>
                            <MudIconButton Icon="@Icons.Material.Filled.Delete"
                                           Color="Color.Error" Size="Size.Small" OnClick="@(() => DeleteTodo(todoItem))" />
                        </MudTd>
                    </RowTemplate>
                </MudTable>
            }
        </MudItem>
    </MudGrid>
</MudContainer>

@* Create Todo Dialog *@
<MudDialog @bind-Visible="showCreateTodoDialog" Options="new DialogOptions { CloseButton = true, FullWidth = true, MaxWidth = MaxWidth.Medium }">
    <TitleContent>
        <MudText Typo="Typo.h6">Create Task</MudText>
    </TitleContent>
    <DialogContent>
        <MudGrid>
            <MudItem xs="12">
                <MudTextField @bind-Value="createTodoModel.Title"
                              Label="Title"
                              Variant="Variant.Outlined"
                              Required="true"
                              Immediate="true" />
            </MudItem>
            <MudItem xs="12">
                <MudTextField @bind-Value="createTodoModel.Description"
                              Label="Description"
                              Variant="Variant.Outlined"
                              Lines="4" />
            </MudItem>
            <MudItem xs="12" md="6">
                <MudSelect @bind-Value="createTodoModel.Status" T="int?" Label="Status" Variant="Variant.Outlined">
                    @foreach (var status in enums?.TodoStatuses?.Where(e => e.Enabled == true) ?? [])
                    {
                        <MudSelectItem T="int?" Value="@status.Id">@status.Value</MudSelectItem>
                    }
                </MudSelect>
            </MudItem>
            <MudItem xs="12" md="6">
                <MudSelect @bind-Value="createTodoModel.Priority" T="int?" Label="Priority" Variant="Variant.Outlined">
                    @foreach (var priority in enums?.TodoPriorities?.Where(e => e.Enabled == true) ?? [])
                    {
                        <MudSelectItem T="int?" Value="@priority.Id">@priority.Value</MudSelectItem>
                    }
                </MudSelect>
            </MudItem>
            <MudItem xs="12" md="6">
                <MudDatePicker Date="ToDateTime(createTodoModel.DueDate)"
                               DateChanged="OnCreateTodoDueDateChanged"
                               Label="Due date"
                               DateFormat="yyyy-MM-dd"
                               Variant="Variant.Outlined"
                               Editable="true"
                               HelperText="Calendar date" />
            </MudItem>
            <MudItem xs="12" md="6">
                <MudTextField T="string"
                              Value="@createTodoDueTime"
                              ValueChanged="OnCreateTodoDueTimeChanged"
                              Label="Due time"
                              Variant="Variant.Outlined"
                              InputType="InputType.Time"
                              HelperText="Local time" />
            </MudItem>
            <MudItem xs="12" md="6">
                <MudTextField @bind-Value="createTodoModel.Assignee"
                              Label="Assignee email"
                              Variant="Variant.Outlined" />
            </MudItem>
        </MudGrid>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="CloseCreateTodoDialog">Cancel</MudButton>
        <MudButton OnClick="CreateTodoFromDialogAsync" Variant="Variant.Filled" Color="Color.Primary">Create</MudButton>
    </DialogActions>
</MudDialog>

@* Export Dialog *@
<MudDialog @bind-Visible="showExportDialog" Options="new DialogOptions { CloseButton = true, MaxWidth = MaxWidth.Small }">
    <TitleContent>
        <MudText Typo="Typo.h6">Export TodoItems</MudText>
    </TitleContent>
    <DialogContent>
        <MudSelect @bind-Value="exportFormat" Label="Export Format" Variant="Variant.Outlined">
            <MudSelectItem Value="@("csv")">CSV</MudSelectItem>
            <MudSelectItem Value="@("json")">JSON</MudSelectItem>
            <MudSelectItem Value="@("xml")">XML</MudSelectItem>
            <MudSelectItem Value="@("excel")">Excel (Single Sheet)</MudSelectItem>
            <MudSelectItem Value="@("pdf")">PDF</MudSelectItem>
        </MudSelect>
        <MudTextField @bind-Value="exportFileName" Label="File Name (optional)" Variant="Variant.Outlined" Margin="Margin.Dense" Class="mt-3" />
        <MudButton OnClick="ExportMulti" Variant="Variant.Outlined" Color="Color.Secondary" FullWidth="true" Class="mt-3">
            Export Multi-Sheet Excel (Grouped by Status)
        </MudButton>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="@(() => showExportDialog = false)">Cancel</MudButton>
        <MudButton OnClick="Export" Variant="Variant.Filled" Color="Color.Primary">Export</MudButton>
    </DialogActions>
</MudDialog>

@* Import Dialog *@
<MudDialog @bind-Visible="showImportDialog" Options="new DialogOptions { CloseButton = true, MaxWidth = MaxWidth.Small }">
    <TitleContent>
        <MudText Typo="Typo.h6">Import TodoItems</MudText>
    </TitleContent>
    <DialogContent>
        <MudSelect @bind-Value="importFormat" Label="Import Format" Variant="Variant.Outlined">
            <MudSelectItem Value="@("csv")">CSV</MudSelectItem>
            <MudSelectItem Value="@("json")">JSON</MudSelectItem>
            <MudSelectItem Value="@("xml")">XML</MudSelectItem>
            <MudSelectItem Value="@("excel")">Excel</MudSelectItem>
        </MudSelect>
        <MudFileUpload T="IBrowserFile" @bind-Files="importFile" Accept=".csv,.json,.xml,.xlsx" MaxFileSize="10485760" Class="mt-3">
            <CustomContent Context="fileUpload">
                <MudButton Variant="Variant.Filled"
                           Color="Color.Primary"
                           StartIcon="@Icons.Material.Filled.CloudUpload"
                           OnClick="@(async () => await fileUpload.OpenFilePickerAsync())">
                    @(importFile == null ? "Select File" : importFile.Name)
                </MudButton>
            </CustomContent>
        </MudFileUpload>
        @if (!string.IsNullOrEmpty(importMessage))
        {
            <MudAlert Severity="@importSeverity" Class="mt-3">@importMessage</MudAlert>
        }
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="@(() => { showImportDialog = false; importMessage = string.Empty; })">Cancel</MudButton>
        <MudButton OnClick="Import" Variant="Variant.Filled" Color="Color.Warning" Disabled="@(importFile == null)">Import</MudButton>
    </DialogActions>
</MudDialog>

@* Attachment Dialog *@
<MudDialog @bind-Visible="showAttachmentsDialog" Options="new DialogOptions { CloseButton = true, FullWidth = true, MaxWidth = MaxWidth.Medium }">
    <TitleContent>
        <MudText Typo="Typo.h6">Attachments for @selectedAttachmentTodo?.Title</MudText>
    </TitleContent>
    <DialogContent>
        <MudStack Spacing="2">
            <MudText Typo="Typo.body2">Upload files into the attachment folder for this todo item.</MudText>

            <MudFileUpload T="IBrowserFile" @bind-Files="attachmentUploadFile" MaxFileSize="@MaxAttachmentUploadSize">
                <CustomContent Context="fileUpload">
                    <MudButton Variant="Variant.Filled"
                               Color="Color.Primary"
                               StartIcon="@Icons.Material.Filled.CloudUpload"
                               OnClick="@(async () => await fileUpload.OpenFilePickerAsync())">
                        @(attachmentUploadFile is null ? "Select file" : attachmentUploadFile.Name)
                    </MudButton>
                </CustomContent>
            </MudFileUpload>

            <MudStack Row="true" Spacing="1" AlignItems="AlignItems.Center">
                <MudButton Variant="Variant.Filled"
                           Color="Color.Success"
                           OnClick="UploadAttachmentAsync"
                           Disabled="@(attachmentUploadFile is null || attachmentDialogLoading)">
                    Upload
                </MudButton>
                <MudButton Variant="Variant.Outlined"
                           Color="Color.Secondary"
                           OnClick="LoadAttachmentsAsync"
                           Disabled="@attachmentDialogLoading">
                    Refresh
                </MudButton>
                @if (attachmentDialogLoading)
                {
                    <MudProgressCircular Indeterminate="true" Size="Size.Small" Color="Color.Info" />
                }
            </MudStack>

            @if (!string.IsNullOrWhiteSpace(attachmentDialogError))
            {
                <MudAlert Severity="Severity.Error" Variant="Variant.Outlined">
                    @attachmentDialogError
                </MudAlert>
            }

            @if (attachments.Count == 0 && !attachmentDialogLoading)
            {
                <MudAlert Severity="Severity.Info" Variant="Variant.Outlined">
                    No attachments uploaded yet.
                </MudAlert>
            }
            else if (attachments.Count > 0)
            {
                <MudTable Items="@attachments" Dense="true" Hover="true">
                    <HeaderContent>
                        <MudTh>Name</MudTh>
                        <MudTh>Type</MudTh>
                        <MudTh>Size</MudTh>
                        <MudTh>Modified</MudTh>
                        <MudTh></MudTh>
                    </HeaderContent>
                    <RowTemplate Context="attachment">
                        <MudTd DataLabel="Name">@attachment.FileName</MudTd>
                        <MudTd DataLabel="Type">@attachment.ContentType</MudTd>
                        <MudTd DataLabel="Size">@FormatAttachmentSize(attachment.Length)</MudTd>
                        <MudTd DataLabel="Modified">@FormatAttachmentTimestamp(attachment.LastModified)</MudTd>
                        <MudTd>
                            <MudTooltip Text="Download">
                                <MudIconButton Icon="@Icons.Material.Filled.Download"
                                               Color="Color.Success"
                                               Size="Size.Small"
                                               OnClick="@(() => DownloadAttachmentAsync(attachment))" />
                            </MudTooltip>
                            <MudTooltip Text="Delete">
                                <MudIconButton Icon="@Icons.Material.Filled.Delete"
                                               Color="Color.Error"
                                               Size="Size.Small"
                                               OnClick="@(() => DeleteAttachmentAsync(attachment))" />
                            </MudTooltip>
                        </MudTd>
                    </RowTemplate>
                </MudTable>
            }
        </MudStack>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="CloseAttachmentsDialog">Close</MudButton>
    </DialogActions>
</MudDialog>

@code {
    private const long MaxAttachmentUploadSize = 8 * 1024 * 1024;
    private const string DefaultDueTimeText = "22:00";

    private sealed class TodoAttachmentItem
    {
        public string FileName { get; set; }

        public long Length { get; set; }

        public string ContentType { get; set; }

        public DateTime? LastModified { get; set; }
    }

    private bool loading = true;
    private ICollection<TodoItemModel> items;
    private EnumerationModel enums;
    private string newTodoTitle;
    private int newTodoPriority = 1;
    private int? filterStatusId;
    private int? filterPriorityId;
    private string filterTitle = string.Empty;
    private SortDirection currentSortDirection = SortDirection.Ascending;
    private bool showCreateTodoDialog = false;
    private TodoItemModel createTodoModel = new();
    private string createTodoDueTime = DefaultDueTimeText;

    private bool showExportDialog = false;
    private bool showImportDialog = false;
    private string exportFormat = "csv";
    private string exportFileName = string.Empty;
    private string importFormat = "csv";
    private IBrowserFile importFile;
    private string importMessage = string.Empty;
    private Severity importSeverity = Severity.Info;
    private bool showAttachmentsDialog = false;
    private bool attachmentDialogLoading;
    private string attachmentDialogError = string.Empty;
    private TodoItemModel selectedAttachmentTodo;
    private IBrowserFile attachmentUploadFile;
    private List<TodoAttachmentItem> attachments = [];

    protected override async Task OnInitializedAsync()
    {
        await LoadEnumerations();
        await LoadItems();
    }

    private async Task LoadEnumerations()
    {
        try
        {
            enums = await Api.Api.Core.Enumerations.GetAsync() ?? new EnumerationModel();
        }
        catch (ApiException ex)
        {
            Snackbar.Add($"Error loading enumerations: {ex.Message}", Severity.Error);
            items = new List<TodoItemModel>();
        }
    }

    private async Task LoadItems()
    {
        loading = true;

        try
        {
            @* var filter2 = FilterModelBuilder.For<TodoItemModel>() // use the typed builder
                .AddFilter(p => p.Status, Common.FilterOperator.Equal, filterStatusId.Value)
                .Build(); *@

            // TODO: re-implement filtering when supported in the API (schema fix!)
            var filter = new BridgingIT.DevKit.Examples.DoFiesta.Presentation.Gen.Models.FilterModel { Filters = [], Orderings = [] };
            if (filterStatusId.HasValue)
            {
                filter.Filters.Add(
                    new FilterModel_filters
                        {
                            Field = "Status",
                            Operator = FilterModel_filters_operator.Eq,
                            Value = new UntypedInteger(filterStatusId.Value)
                        });
            }

            if (filterPriorityId.HasValue)
            {
                filter.Filters.Add(
                    new FilterModel_filters
                        {
                            Field = "Priority",
                            Operator = FilterModel_filters_operator.Gte,
                            Value = new UntypedInteger(filterPriorityId.Value)
                        });
            }

            if (!string.IsNullOrWhiteSpace(filterTitle))
            {
                filter.Filters.Add(
                    new FilterModel_filters
                        {
                            Field = "Title",
                            Operator = FilterModel_filters_operator.Contains,
                            Value = new UntypedString(filterTitle)
                        });
            }

            // set ordering
            filter.Orderings.Add(new BridgingIT.DevKit.Examples.DoFiesta.Presentation.Gen.Models.FilterOrderCriteria
                {
                    Field = "DueDate",
                    Direction = currentSortDirection == SortDirection.Ascending
                                    ? BridgingIT.DevKit.Examples.DoFiesta.Presentation.Gen.Models.OrderDirection.Asc
                                    : BridgingIT.DevKit.Examples.DoFiesta.Presentation.Gen.Models.OrderDirection.Desc
                });

            // filter.Filters.Add(
            //     new Client.FilterCriteria
            //         {
            //             CustomType = FilterCriteriaCustomType.Namedspecification,
            //             SpecificationName = "TodoItemIsNotDeleted",
            //         });

            items = await Api.Api.Core.Todoitems.Search.PostAsync(filter) ?? [];
        }
        catch (ApiException ex)
        {
            Snackbar.Add($"Error loading items: {ex.Message}", Severity.Error);
            enums = new EnumerationModel();
        }
        finally
        {
            loading = false;
        }
    }

    private async Task OnStatusFilterChanged(int? statusId)
    {
        filterStatusId = statusId;
        await LoadItems();
    }

    private async Task OnStatusChipChanged(int id)
    {
        filterStatusId = id;

        if (id == 0)
            filterStatusId = null;

        await LoadItems();
    }

    private async Task OnPriorityChipChanged(int id)
    {
        filterPriorityId = id;

        if (id == 0)
            filterPriorityId = null;

        await LoadItems();
    }

    private async Task OpenAttachmentsDialogAsync(TodoItemModel item)
    {
        selectedAttachmentTodo = item;
        showAttachmentsDialog = true;
        attachmentUploadFile = null;
        await LoadAttachmentsAsync();
    }

    private void CloseAttachmentsDialog()
    {
        showAttachmentsDialog = false;
        selectedAttachmentTodo = null;
        attachmentUploadFile = null;
        attachmentDialogError = string.Empty;
        attachments.Clear();
    }

    private async Task LoadAttachmentsAsync()
    {
        if (string.IsNullOrWhiteSpace(selectedAttachmentTodo?.Id))
        {
            attachments.Clear();
            return;
        }

        attachmentDialogLoading = true;
        attachmentDialogError = string.Empty;

        try
        {
            var response = await Http.GetFromJsonAsync<List<TodoAttachmentItem>>(
                $"api/core/todoitems/{selectedAttachmentTodo.Id}/attachments");
            attachments = response?
                .OrderBy(attachment => attachment.FileName, StringComparer.OrdinalIgnoreCase)
                .ToList() ?? [];
        }
        catch (Exception ex)
        {
            attachments.Clear();
            attachmentDialogError = $"Unable to load attachments: {ex.Message}";
            Snackbar.Add(attachmentDialogError, Severity.Error);
        }
        finally
        {
            attachmentDialogLoading = false;
        }
    }

    private async Task UploadAttachmentAsync()
    {
        if (attachmentUploadFile is null || string.IsNullOrWhiteSpace(selectedAttachmentTodo?.Id))
        {
            return;
        }

        attachmentDialogLoading = true;
        attachmentDialogError = string.Empty;

        try
        {
            using var content = new MultipartFormDataContent();
            await using var stream = attachmentUploadFile.OpenReadStream(maxAllowedSize: MaxAttachmentUploadSize);
            using var streamContent = new StreamContent(stream);
            streamContent.Headers.ContentType = CreateContentTypeHeader(attachmentUploadFile.ContentType);
            content.Add(streamContent, "file", attachmentUploadFile.Name);

            var response = await Http.PostAsync($"api/core/todoitems/{selectedAttachmentTodo.Id}/attachments", content);
            if (!response.IsSuccessStatusCode)
            {
                attachmentDialogError = $"Upload failed: {response.StatusCode}";
                Snackbar.Add(attachmentDialogError, Severity.Error);
                return;
            }

            Snackbar.Add($"Uploaded '{attachmentUploadFile.Name}'.", Severity.Success);
            attachmentUploadFile = null;
            await LoadAttachmentsAsync();
        }
        catch (Exception ex)
        {
            attachmentDialogError = $"Unable to upload attachment: {ex.Message}";
            Snackbar.Add(attachmentDialogError, Severity.Error);
        }
        finally
        {
            attachmentDialogLoading = false;
        }
    }

    private async Task DownloadAttachmentAsync(TodoAttachmentItem attachment)
    {
        if (string.IsNullOrWhiteSpace(selectedAttachmentTodo?.Id))
        {
            return;
        }

        try
        {
            var url = $"api/core/todoitems/{selectedAttachmentTodo.Id}/attachments/content?fileName={Uri.EscapeDataString(attachment.FileName)}";
            var response = await Http.GetAsync(url);
            response.EnsureSuccessStatusCode();

            var stream = await response.Content.ReadAsStreamAsync();
            await DownloadFile(stream, attachment.FileName);
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Unable to download attachment: {ex.Message}", Severity.Error);
        }
    }

    private async Task DeleteAttachmentAsync(TodoAttachmentItem attachment)
    {
        if (string.IsNullOrWhiteSpace(selectedAttachmentTodo?.Id))
        {
            return;
        }

        attachmentDialogLoading = true;
        attachmentDialogError = string.Empty;

        try
        {
            var url = $"api/core/todoitems/{selectedAttachmentTodo.Id}/attachments?fileName={Uri.EscapeDataString(attachment.FileName)}";
            var response = await Http.DeleteAsync(url);
            if (!response.IsSuccessStatusCode)
            {
                attachmentDialogError = $"Delete failed: {response.StatusCode}";
                Snackbar.Add(attachmentDialogError, Severity.Error);
                return;
            }

            Snackbar.Add($"Deleted '{attachment.FileName}'.", Severity.Success);
            await LoadAttachmentsAsync();
        }
        catch (Exception ex)
        {
            attachmentDialogError = $"Unable to delete attachment: {ex.Message}";
            Snackbar.Add(attachmentDialogError, Severity.Error);
        }
        finally
        {
            attachmentDialogLoading = false;
        }
    }

    private async Task AddNewTodo()
    {
        if (string.IsNullOrWhiteSpace(newTodoTitle))
            return;

        loading = true;
        var newItem = new TodoItemModel
            {
                Title = newTodoTitle,
                Status = enums.TodoStatuses.First().Id,
                Priority = newTodoPriority //enums.TodoPriorities.First().Id
            };
        newTodoTitle = string.Empty;
        items.Add(newItem);

        try
        {

            _ = await Api.Api.Core.Todoitems.PostAsync(newItem);

            Snackbar.Add("Added successfully", Severity.Success);
            await LoadItems();
        }
        // catch (ApiException<ProblemDetails> pex)
        // {
        //     Snackbar.Add($"Problem adding todo: [{pex.StatusCode}] {pex.Result.Detail}", Severity.Warning);
        // }
        catch (ApiException ex)
        {
            items.Remove(newItem);
            Snackbar.Add($"Error adding todo: [{ex.ResponseStatusCode}] {ex.Message}", Severity.Error);
        }
        finally
        {
            loading = false;
        }
    }

    private void OpenCreateTodoDialog()
    {
        this.ResetCreateTodoModel();
        this.showCreateTodoDialog = true;
    }

    private void CloseCreateTodoDialog()
    {
        this.showCreateTodoDialog = false;
    }

    private Task OnCreateTodoDueDateChanged(DateTime? date)
    {
        this.createTodoModel.DueDate = CombineDueDateAndTime(date, this.createTodoDueTime);
        return Task.CompletedTask;
    }

    private Task OnCreateTodoDueTimeChanged(string value)
    {
        this.createTodoDueTime = string.IsNullOrWhiteSpace(value) ? DefaultDueTimeText : value;
        this.createTodoModel.DueDate = CombineDueDateAndTime(ToDateTime(this.createTodoModel.DueDate), this.createTodoDueTime);
        return Task.CompletedTask;
    }

    private async Task OnTodoDueDateChangedAsync(TodoItemModel item, DateTime? date)
    {
        item.DueDate = CombineDueDateAndTime(date, FormatDueTime(item.DueDate));
        await UpdateTodo(item);
    }

    private async Task OnTodoDueTimeChangedAsync(TodoItemModel item, string value)
    {
        item.DueDate = CombineDueDateAndTime(ToDateTime(item.DueDate), value);
        await UpdateTodo(item);
    }

    private async Task CreateTodoFromDialogAsync()
    {
        if (string.IsNullOrWhiteSpace(this.createTodoModel.Title))
        {
            this.Snackbar.Add("Title is required.", Severity.Warning);
            return;
        }

        loading = true;

        try
        {
            await Api.Api.Core.Todoitems.PostAsync(this.createTodoModel);

            this.showCreateTodoDialog = false;
            this.Snackbar.Add("Task created successfully", Severity.Success);
            this.ResetCreateTodoModel();
            await LoadItems();
        }
        catch (ApiException ex)
        {
            this.Snackbar.Add($"Error creating todo: [{ex.ResponseStatusCode}] {ex.Message}", Severity.Error);
        }
        finally
        {
            loading = false;
        }
    }

    private async Task UpdateTodo(TodoItemModel item)
    {
        loading = true;
        Console.WriteLine($"Updating todo: {item.Id}");

        try
        {
            await Api.Api.Core.Todoitems[item.Id].PutAsync(item);

            Snackbar.Add("Updated successfully", Severity.Success);
            await LoadItems();
        }
        catch (ApiException ex)
        {
            Snackbar.Add($"Error updating todo: {ex.Message}", Severity.Error);
        }
        finally
        {
            loading = false;
        }
    }

    private static DateTimeOffset? CombineDueDateAndTime(DateTime? date, string timeText)
    {
        if (!date.HasValue)
        {
            return null;
        }

        return new DateTimeOffset(date.Value.Date.Add(ParseDueTime(timeText)));
    }

    private static string FormatDueTime(DateTimeOffset? date)
    {
        return date.HasValue
            ? date.Value.LocalDateTime.ToString("HH:mm")
            : DefaultDueTimeText;
    }

    private static DateTime? ToDateTime(DateTimeOffset? date)
    {
        return date?.LocalDateTime;
    }

    private static TimeSpan ParseDueTime(string timeText)
    {
        return TimeSpan.TryParse(timeText, out var time)
            ? new TimeSpan(time.Hours, time.Minutes, 0)
            : TimeSpan.Parse(DefaultDueTimeText);
    }

    private void ResetCreateTodoModel()
    {
        this.createTodoModel = new TodoItemModel
        {
            Status = enums?.TodoStatuses?.FirstOrDefault(e => e.Enabled == true)?.Id ?? 1,
            Priority = enums?.TodoPriorities?.FirstOrDefault(e => e.Enabled == true)?.Id ?? newTodoPriority,
        };
        this.createTodoDueTime = DefaultDueTimeText;
    }

    private async Task CompleteAll()
    {
        loading = true;
        Console.WriteLine($"Completing all");

        try
        {
            await Api.Api.Core.Todoitems.Actions.Completeall.PostAsync();

            Snackbar.Add("Completed successfully", Severity.Success);
            await LoadItems();
        }
        catch (ApiException ex)
        {
            Snackbar.Add($"Error completing todos: {ex.Message}", Severity.Error);
        }
        finally
        {
            loading = false;
        }
    }

    private async Task DeleteTodo(TodoItemModel item)
    {
        loading = true;
        var removeItem = items.FirstOrDefault(e => e.Id == item.Id);
        items.Remove(removeItem);

        try
        {
            await Api.Api.Core.Todoitems[item.Id].DeleteAsync();

            Snackbar.Add("Deleted successfully", Severity.Success);
            await LoadItems();
        }
        catch (ApiException ex)
        {
            items.Add(removeItem);
            Snackbar.Add($"Error deleting todo: {ex.Message}", Severity.Error);
        }
        finally
        {
            loading = false;
        }
    }

    private async Task OnDueDateSortClick()
    {
        currentSortDirection = currentSortDirection switch
        {
            SortDirection.None => SortDirection.Ascending,
            SortDirection.Ascending => SortDirection.Descending,
            SortDirection.Descending => SortDirection.None,
            _ => SortDirection.None
        };

        await LoadItems();
    }

    private (string icon, Color color) GetStatusIcon(int statusId)
    {
        return statusId switch
        {
            1 => (Icons.Material.Filled.FiberNew, Color.Info),        // New
            2 => (Icons.Material.Filled.RunCircle, Color.Warning),    // InProgress
            3 => (Icons.Material.Filled.CheckCircle, Color.Success),  // Completed
            _ => (Icons.Material.Filled.Circle, Color.Default)        // Default
        };
    }

    private async Task Export()
    {
        try
        {
            var fileName = string.IsNullOrWhiteSpace(exportFileName)
                ? $"todoitems-{DateTime.Now:yyyy-MM-dd}.{exportFormat}"
                : exportFileName;

            var url = $"api/core/todoitems/dataporter/export/{exportFormat}?fileName={Uri.EscapeDataString(fileName)}";
            var response = await Http.GetAsync(url);

            if (response.IsSuccessStatusCode)
            {
                var stream = await response.Content.ReadAsStreamAsync();
                await DownloadFile(stream, fileName);
                Snackbar.Add($"Exported successfully to {exportFormat.ToUpper()}", Severity.Success);
                showExportDialog = false;
            }
            else
            {
                Snackbar.Add($"Export failed: {response.StatusCode}", Severity.Error);
            }
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error exporting: {ex.Message}", Severity.Error);
        }
    }

    private async Task ExportMulti()
    {
        try
        {
            var fileName = string.IsNullOrWhiteSpace(exportFileName)
                ? $"todoitems-multi-{DateTime.Now:yyyy-MM-dd}.xlsx"
                : exportFileName;

            var url = $"api/core/todoitems/dataporter/export/multi?fileName={Uri.EscapeDataString(fileName)}";
            var response = await Http.GetAsync(url);

            if (response.IsSuccessStatusCode)
            {
                var stream = await response.Content.ReadAsStreamAsync();
                await DownloadFile(stream, fileName);
                Snackbar.Add("Exported successfully to multi-sheet Excel", Severity.Success);
                showExportDialog = false;
            }
            else
            {
                Snackbar.Add($"Export failed: {response.StatusCode}", Severity.Error);
            }
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error exporting: {ex.Message}", Severity.Error);
        }
    }

    private async Task Import()
    {
        if (importFile == null)
        {
            importMessage = "Please select a file";
            importSeverity = Severity.Warning;
            return;
        }

        loading = true;
        importMessage = "Importing...";
        importSeverity = Severity.Info;

        try
        {
            using var content = new MultipartFormDataContent();
            using var stream = importFile.OpenReadStream(maxAllowedSize: 10485760);
            using var streamContent = new StreamContent(stream);
            streamContent.Headers.ContentType = CreateContentTypeHeader(importFile.ContentType);
            content.Add(streamContent, "file", importFile.Name);

            var url = $"api/core/todoitems/dataporter/import/{importFormat}";
            var response = await Http.PostAsync(url, content);

            if (response.IsSuccessStatusCode)
            {
                var responseContent = await response.Content.ReadAsStringAsync();
                importMessage = $"Import successful! Processed data from {importFormat.ToUpper()} file.";
                importSeverity = Severity.Success;
                Snackbar.Add(importMessage, Severity.Success);
                await LoadItems();
                showImportDialog = false;
                importMessage = string.Empty;
            }
            else if (response.StatusCode == System.Net.HttpStatusCode.BadRequest)
            {
                var errorContent = await response.Content.ReadAsStringAsync();
                importMessage = $"Import completed with warnings: {errorContent}";
                importSeverity = Severity.Warning;
                Snackbar.Add(importMessage, Severity.Warning);
            }
            else
            {
                importMessage = $"Import failed: {response.StatusCode}";
                importSeverity = Severity.Error;
                Snackbar.Add(importMessage, Severity.Error);
            }
        }
        catch (Exception ex)
        {
            importMessage = $"Error importing: {ex.Message}";
            importSeverity = Severity.Error;
            Snackbar.Add(importMessage, Severity.Error);
        }
        finally
        {
            loading = false;
        }
    }

    private async Task DownloadFile(Stream stream, string fileName)
    {
        using var streamRef = new DotNetStreamReference(stream);
        await JS.InvokeVoidAsync("downloadFileFromStream", fileName, streamRef);
    }

    private static string FormatAttachmentSize(long length)
    {
        const double kiloByte = 1024;
        const double megaByte = kiloByte * 1024;

        return length switch
        {
            >= (long)megaByte => $"{length / megaByte:0.##} MB",
            >= (long)kiloByte => $"{length / kiloByte:0.##} KB",
            _ => $"{length} B"
        };
    }

    private static string FormatAttachmentTimestamp(DateTime? timestamp)
        => timestamp?.ToLocalTime().ToString("yyyy-MM-dd HH:mm") ?? "-";

    private static MediaTypeHeaderValue CreateContentTypeHeader(string contentType) =>
        !string.IsNullOrWhiteSpace(contentType) && MediaTypeHeaderValue.TryParse(contentType, out var mediaType)
            ? mediaType
            : new MediaTypeHeaderValue("application/octet-stream");
}
